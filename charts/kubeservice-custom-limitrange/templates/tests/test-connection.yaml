apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeservice-custom-limitrange.fullname" . }}-test-connection"
  labels:
    {{- include "kubeservice-custom-limitrange.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeservice-custom-limitrange.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
