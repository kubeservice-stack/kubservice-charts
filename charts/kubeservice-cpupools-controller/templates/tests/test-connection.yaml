apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeservice-cpupools-controller.fullname" . }}-test-connection"
  labels:
    {{- include "kubeservice-cpupools-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeservice-cpupools-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
